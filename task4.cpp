#include <iostream>
using namespace std;
main()
{
    int product;
    int size;
    int number;
    cout<<"enter the size of array:";
    cin>>size;
    int numbers[size];
    for(int i=0;i<size;i=i+1)
    {
      cout<<"enter the number:";
      cin>>numbers[i];
        
   }
     cout<<"enter the number to multiply";
     cin>>number;
    for(int i=size-1; i>=0;i=i-1)
   {
    number[i]-p0=numbers*numbers[i];
    cout<<numbers[i] <<" ";
   }
}